// Remove all previous code and just set initial state
document.addEventListener('DOMContentLoaded', () => {
    const percentage = document.querySelector('.percentage');
    percentage.textContent = '70%';
}); 